document.getElementById("go-back").addEventListener("click" ,() =>{
    history.back();
})

const playerNavButton = document.getElementsById("navigation");

playerNavButton.addEventListener("click", () => {
    playerNavButton.
})